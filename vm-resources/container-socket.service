[Unit]
Description=Container Socket Service
After=network.target
StartLimitIntervalSec=0

[Service]
Type=simple
Restart=always
RestartSec=1
User=florian
ExecStart=nc -lkU /tmp/mySocket.sock

[Install]
WantedBy=multi-user.target
